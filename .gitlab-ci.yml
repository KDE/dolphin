# SPDX-FileCopyrightText: 2020-2026 KDE Contributors
#
# SPDX-License-Identifier: CC0-1.0

include:
  - project: sysadmin/ci-utilities
    file:
      # DEBUG (throwaway): only the Qt 6.11 Linux pipeline, where dolphinmainwindowtest hangs.
      # All other pipelines (Qt-next/6.12, FreeBSD, Windows, Flatpak, Craft, lints) removed to
      # iterate fast on the hang. Revert this together with the QTEST_FUNCTION_TIMEOUT change.
      - /gitlab-templates/linux-qt6.yml
