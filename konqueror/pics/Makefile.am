data_DATA = treeclose.xpm treeopen.xpm
datadir = $(kde_datadir)/konqueror/pics

# This one is used by kdesktop as well
icon_DATA = kmultiple.xpm
icondir = $(kde_icondir)

EXTRA_DIST = $(data_DATA) $(icon_DATA)

